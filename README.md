通过Apifox的地址自动生成api 和 types

开发调试 
在cli项目根目录下执行 
```javascript
 npm link / unlink
 npm install / uninstall -g
```
修改cli本地代码执行gao-cli命令
```javascript
gao-cli -v / --version
gao-cli -h / --help
gao-cli init
```